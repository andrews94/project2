<h2>Edit Champion</h2>
<%= form_for @champion do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>

 <%= f.label :Image %>
 <%= f.text_field :img_url %>

 <%= f.submit %>
 <% end %>

<!-- unlinked in your application; link it from champsion show view maybe.  -->
